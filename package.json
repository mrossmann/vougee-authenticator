{
  "name": "vougee-authenticator",
  "version": "1.0.0",
  "description": "vougee authentication library",
  "main": "dist/vougee-authenticator.cjs.js",
  "module": "dist/vougee-authenticator.esm.js",
  "browser": "dist/vougee-authenticator.umd.js",
  "types": "build/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rollup -c",
    "build:types": "rm -rf build && tsc -p ./tsconfig.json --outDir build --declaration true && api-extractor run",
    "docs": "typedoc src --out docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Rossmann",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^22.0.2",
    "@rollup/plugin-node-resolve": "^14.1.0",
    "@rollup/plugin-typescript": "^8.5.0",
    "eslint": "^8.24.0",
    "rollup": "^2.79.1",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@microsoft/api-extractor": "^7.32.0",
    "typedoc": "^0.23.15"
  }
}
