export enum EProtocol {
  HTTPS = "https",
  HTTP = "http"
}

export enum ESubDomain {
  VOUGEE = "vougee.id",
  ZENIQ = "zeniq.id"
}

export enum EAddress {
  ZENIQ = "&t=1&tZENIQ-1",
  ETH = "&t=1&tETH",
  BTC = "&t=1&tBTC"
}